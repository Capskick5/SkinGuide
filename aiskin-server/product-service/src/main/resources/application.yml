spring:
  application:
    name: product-service
  mongodb:
    uri: ${MONGODB_URI:mongodb://localhost:27017/aiskin_product}
  data:
    mongodb:
      auto-index-creation: true

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true

logging:
  level:
    mss.productservice: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
  api-docs:
    path: /v3/api-docs
